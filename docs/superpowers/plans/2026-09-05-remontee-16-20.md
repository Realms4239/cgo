# Remontée 16/20 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Preuve pilote P2 BBR crédible (valid-only + IC95 + Mann-Whitney + Cliff) avec parité mémoire × interface, sans nouvelle mesure.

**Architecture:** Source unique Go `pkg/metrics/stats.go` (seed 42) réutilisée par l'API (cache précalculé) et reflétée en miroir dans `book/extract-stats.js`. Extensions API strictement additives. Textes book recadrés instrument + bornes P3/P4.

**Tech Stack:** Go 1.26, Node 22 (scripts book, zéro dép), React web, Bubbletea TUI.

## Global Constraints

- P0 placeholders `[[ ]]` : NE RIEN CHANGER (by design).
- Worktree `.worktrees/overhaul1.2.2` sur HEAD ; réutiliser `node_modules` + `GOMODCACHE`, zéro `npm install` / `go download` (builds avec `GOPROXY=off`).
- API : champs additifs seuls (anciens champs intacts).
- Stats : bootstrap 10k, seed 42, MW bilatéral α=0,05, Cliff Vargha-Delaney (<0,11 négligeable, <0,28 petit, <0,43 moyen, sinon grand).
- Aucun secret (VM altfloat : env seul, jamais commité).
- Commits : un par tâche, messages `feat/fix/docs:` concis.

---

### Task 1: `pkg/metrics/stats.go` + tests (source unique)

**Files:**
- Create: `pkg/metrics/stats.go`
- Create: `pkg/metrics/stats_test.go`

**Interfaces:**
- Consumes: rien (stdlib seul : `math`, `sort`).
- Produces: `Median(xs []float64) float64`, `Quartiles(xs []float64) (q1, med, q3 float64)`, `IQR(xs []float64) float64`, `BootstrapMedianCI95(xs []float64, resamples int, seed uint64) (lo, hi float64)`, `MannWhitneyTwoSided(a, b []float64) (u, p float64)`, `CliffDelta(a, b []float64) float64`, `CliffInterp(d float64) string`.

- [ ] **Step 1: Write the failing test**

```go
package metrics

import "testing"

func TestMedianIQR(t *testing.T) {
	if got := Median([]float64{3, 1, 2}); got != 2 {
		t.Fatalf("median=%v want 2", got)
	}
	if got := IQR([]float64{1, 2, 3, 4}); got != 2 {
		t.Fatalf("iqr=%v want 2", got)
	}
}

func TestCliffDeltaObvious(t *testing.T) {
	a := []float64{1, 2, 3}
	b := []float64{4, 5, 6}
	if d := CliffDelta(a, b); d != -1 {
		t.Fatalf("delta=%v want -1", d)
	}
	if s := CliffInterp(-1); s != "grand" {
		t.Fatalf("interp=%q want grand", s)
	}
}

func TestMannWhitneySeparated(t *testing.T) {
	a := []float64{60.2, 55.6, 58.1, 57.0, 59.3}
	b := []float64{96.2, 98.1, 97.5, 98.0, 96.8}
	_, p := MannWhitneyTwoSided(a, b)
	if p >= 0.05 {
		t.Fatalf("p=%v want <0.05", p)
	}
}

func TestBootstrapCISeeded(t *testing.T) {
	xs := []float64{60.2, 55.6, 58.1, 57.0, 59.3, 61.0}
	lo1, hi1 := BootstrapMedianCI95(xs, 10000, 42)
	lo2, hi2 := BootstrapMedianCI95(xs, 10000, 42)
	if lo1 != lo2 || hi1 != hi2 || !(lo1 <= 58.1 && 58.1 <= hi1) {
		t.Fatalf("ci not seeded/stable: [%v,%v] vs [%v,%v]", lo1, hi1, lo2, hi2)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `GOPROXY=off go test ./pkg/metrics/ -run 'TestMedianIQR|TestCliffDeltaObvious|TestMannWhitneySeparated|TestBootstrapCISeeded' -v`
Expected: FAIL with "undefined" (stats.go n'existe pas).

- [ ] **Step 3: Write minimal implementation**

```go
// stats.go — inférence minimale pour la preuve pilote (seed fixe 42).
package metrics

import (
	"math"
	"sort"
)

func sortedCopy(xs []float64) []float64 {
	s := append([]float64(nil), xs...)
	sort.Float64s(s)
	return s
}

func quantileSorted(s []float64, q float64) float64 {
	if len(s) == 0 {
		return math.NaN()
	}
	if len(s) == 1 {
		return s[0]
	}
	pos := float64(len(s)-1) * q
	lo := int(math.Floor(pos))
	hi := int(math.Ceil(pos))
	if lo == hi {
		return s[lo]
	}
	return s[lo] + (s[hi]-s[lo])*(pos-float64(lo))
}

// Median — médiane (interpolation linéaire, même convention que Summarize).
func Median(xs []float64) float64 { return quantileSorted(sortedCopy(xs), 0.5) }

// Quartiles — Q1, médiane, Q3.
func Quartiles(xs []float64) (q1, med, q3 float64) {
	s := sortedCopy(xs)
	return quantileSorted(s, 0.25), quantileSorted(s, 0.5), quantileSorted(s, 0.75)
}

// IQR — Q3−Q1.
func IQR(xs []float64) float64 {
	q1, _, q3 := Quartiles(xs)
	return q3 - q1
}

// lcg — Park-Miller minimal pour un bootstrap seedé sans dépendance.
type lcg struct{ s uint64 }

func (r *lcg) next(n int) int {
	r.s = (r.s * 48271) % 2147483647
	return int(r.s % uint64(n))
}

// BootstrapMedianCI95 — IC95 percentile de la médiane (seed fixe → stable).
func BootstrapMedianCI95(xs []float64, resamples int, seed uint64) (lo, hi float64) {
	if len(xs) == 0 || resamples <= 0 {
		return math.NaN(), math.NaN()
	}
	r := &lcg{s: seed % 2147483647}
	if r.s == 0 {
		r.s = 1
	}
	meds := make([]float64, resamples)
	buf := make([]float64, len(xs))
	for i := 0; i < resamples; i++ {
		for j := range buf {
			buf[j] = xs[r.next(len(xs))]
		}
		meds[i] = quantileSorted(sortedCopy(buf), 0.5)
	}
	sort.Float64s(meds)
	return quantileSorted(meds, 0.025), quantileSorted(meds, 0.975)
}

// MannWhitneyTwoSided — U + p bilatéral (approx. normale, correction de continuité).
func MannWhitneyTwoSided(a, b []float64) (u, p float64) {
	type v struct {
		x float64
		g int
	}
	all := make([]v, 0, len(a)+len(b))
	for _, x := range a {
		all = append(all, v{x, 0})
	}
	for _, x := range b {
		all = append(all, v{x, 1})
	}
	sort.Slice(all, func(i, j int) bool { return all[i].x < all[j].x })
	var ra float64
	i := 0
	for i < len(all) {
		j := i
		for j+1 < len(all) && all[j+1].x == all[i].x {
			j++
		}
		avg := float64(i+1+j+1) / 2 // rangs 1-based moyens sur ex æquo
		for k := i; k <= j; k++ {
			if all[k].g == 0 {
				ra += avg
			}
		}
		i = j + 1
	}
	n1, n2 := float64(len(a)), float64(len(b))
	u1 := ra - n1*(n1+1)/2
	u = u1
	mu := n1 * n2 / 2
	sd := math.Sqrt(n1 * n2 * (n1 + n2 + 1) / 12)
	if sd == 0 {
		return u, 1
	}
	z := (math.Abs(u1-mu) - 0.5) / sd // continuité
	p = math.Erfc(z / math.Sqrt2)
	return u, p
}

// CliffDelta — delta de Cliff (b−a dominant → +1).
func CliffDelta(a, b []float64) float64 {
	if len(a) == 0 || len(b) == 0 {
		return math.NaN()
	}
	var more, less float64
	for _, x := range a {
		for _, y := range b {
			switch {
			case y > x:
				more++
			case y < x:
				less++
			}
		}
	}
	return (more - less) / (float64(len(a)) * float64(len(b)))
}

// CliffInterp — seuils Vargha-Delaney sur |d|.
func CliffInterp(d float64) string {
	a := math.Abs(d)
	switch {
	case a < 0.11:
		return "négligeable"
	case a < 0.28:
		return "petit"
	case a < 0.43:
		return "moyen"
	default:
		return "grand"
	}
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `GOPROXY=off go test ./pkg/metrics/ -v`
Expected: PASS (tout le package, pas de régression Summarize/Cost).

- [ ] **Step 5: Commit**

```bash
git add pkg/metrics/stats.go pkg/metrics/stats_test.go
git commit -m "feat(metrics): mediane IQR bootstrap-IC95 MW-bilateral Cliff-delta (seed 42)"
```

### Task 2: `failed_gates` futur (writer + campagne)

**Files:**
- Modify: `pkg/campagne/writer.go:25-31,125-128`
- Modify: `pkg/campagne/campagne.go:475-491` (collecter les portes en échec)
- Test: `pkg/campagne/failed_gates_test.go` (create)

**Interfaces:**
- Consumes: `model.Gate` consts, `GateCount` (existant).
- Produces: `quarantineEntry.FailedGates []string`, `ev` inchangé pour archives passées.

- [ ] **Step 1: Write the failing test**

```go
package campagne

import (
	"encoding/json"
	"os"
	"path/filepath"
	"testing"

	"github.com/Realms4239/cgo/pkg/model"
)

func TestQuarantineLogsFailedGates(t *testing.T) {
	dir := t.TempDir()
	w, err := OpenRun(filepath.Join(dir, "run-x"))
	if err != nil {
		t.Fatal(err)
	}
	ev := model.Event{RunID: "run-x", EventID: 1, Profile: "P2", Qdisc: "cake", CC: "bbr", GateStatus: model.GateInvalid}
	ev.FailedGates = []string{"G4ThroughputCoherent"}
	if err := w.Append(ev); err != nil {
		t.Fatal(err)
	}
	if err := w.Freeze("cfg"); err != nil {
		t.Fatal(err)
	}
	b, _ := os.ReadFile(filepath.Join(dir, "run-x", "quarantine.json"))
	var q []map[string]any
	if err := json.Unmarshal(b, &q); err != nil {
		t.Fatal(err)
	}
	if q[0]["failed_gates"] == nil {
		t.Fatalf("failed_gates absent: %s", string(b))
	}
}
```

NOTE: exige `FailedGates []string` sur `model.Event` — ajouter `FailedGates []string json:"failed_gates,omitempty"` dans `pkg/model/types.go` près de `GateStatus` (même tâche, fichier Modify supplémentaire).

- [ ] **Step 2: Run test to verify it fails**

Run: `GOPROXY=off go test ./pkg/campagne/ -run TestQuarantineLogsFailedGates -v`
Expected: FAIL (`FailedGates` indéfini).

- [ ] **Step 3: Write minimal implementation**

1. `pkg/model/types.go` : ajouter `FailedGates []string \`json:"failed_gates,omitempty"\`` après `GateStatus`.
2. `pkg/campagne/campagne.go` : dans la boucle 476-490, accumuler `var failed []string` avec le nom de porte (`"G0TargetReachable"`, …) quand `!*b`, puis `ev.FailedGates = failed` avant `ev.GateStatus = status`.
3. `pkg/campagne/writer.go` : ajouter `FailedGates []string \`json:"failed_gates,omitempty"\`` à `quarantineEntry` et le renseigner dans `Append`.

- [ ] **Step 4: Run test to verify it passes**

Run: `GOPROXY=off go test ./pkg/campagne/ ./pkg/model/ -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add pkg/model/types.go pkg/campagne/campagne.go pkg/campagne/writer.go pkg/campagne/failed_gates_test.go
git commit -m "feat(campagne): failed_gates en quarantaine (futur, archives intactes)"
```

### Task 3: `book/extract-stats.js` valid-only + inférentiel + rebuild

**Files:**
- Modify: `book/extract-stats.js`
- Rebuild: `book/data/stats.json` (généré, ignoré par git — régénérer, pas committer)

**Interfaces:**
- Consumes: `book/data/runs/*/aqm_eval.csv`.
- Produces: `cells[]` avec `n (valid-only)`, `q1,q3,iqr`, `deadline_ci95`, `small_ci95`, + bloc `infer_p2bbr {mw_deadline_p, mw_small_p, cliff_deadline, cliff_small, cliff_interp}` ; miroir exact des formules Go (quantile linéaire, LCG Park-Miller seed 42, MW approx normale).

- [ ] **Step 1: filtrer valid-only + IQR (test manuel)**

Remplacer `if (r.gate_status === 'invalid') continue;` par `if (r.gate_status !== 'valid') continue;`, ajouter `q1/med/q3/iqr` (quantile linéaire identique à `quantileSorted`), `bootstrapCI` (LCG 48271/2^31−1, 10k, seed 42), `mannWhitney` + `cliff` miroirs. Recalculer `infer` sur P2 BBR valid-only pfifo vs cake (deadline_ok_pct + small_p95_ms).

- [ ] **Step 2: Run + vérifier**

Run: `node extract-stats.js`
Expected: `runs: 153 | lignes: 412 | valid: 136 | ...` + nouveau `n` P2 BBR valid-only (≤16/12/12), `infer_p2bbr` avec p<0,05 et cliff≈±1 (grand).

- [ ] **Step 3: parité JS/Go**

Run: petit script comparant `Median/IQR/CI(p2 pfifo deadline)` JS vs `go test` helper — écart 0 (arrondi 0,1).

- [ ] **Step 4: Commit (script seul, stats.json ignoré)**

```bash
git add -f book/extract-stats.js 2>/dev/null || git add book/extract-stats.js
git commit -m "feat(book): valid-only + IQR IC95 MW Cliff (miroir Go, seed 42)"
```

NOTE: `book/` est ignoré par `.gitignore` — si `git add` refuse, forcer avec `-f` reste conforme (fichiers déjà suivis en amont ? vérifier `git check-ignore`). Si le dépôt principal ne suit pas `book/`, reporter les mêmes edits sur `C:/cgo/book` après.

### Task 4: Textes mémoire (recadrage + quarantaine + coût + P3/P4 + biblio + résumé)

**Files:**
- Modify: `book/content/04-pratique.js` (≈§ final220 + agrégats + wasted220 + reco + limites)
- Modify: `book/content/03-partie2.js` (note G4-Mathis §portes + paliers, matériels inchangés P0)
- Modify: `book/content/07-refs.js` (11→~28)
- Modify: `book/content/00-front.js` (résumé/abstract chiffrés à n — SANS toucher titlePage `[[ ]]`)

**Interfaces:** Consumes: `book/data/stats.json` régénéré (n valid-only, IC, p, delta).

- [ ] **Step 1: 04-pratique** — remplacer le § agrégats (« Sur seize répétitions… ») par n valid-only + IQR + IC95 + « MW pfifo vs cake p=…, Cliff grand » ; ajouter colonne n au tableau final220 (légende) ; wasted220 : audit `wasted=drops×1448` + explication outlier P1|cake|bbr + tableau sensibilité 6 paliers ; reco différenciée (fibre fq_codel / cellulaire cake 90 % + re-mesure / VSAT régularité / BBR tester / sentinelle, MikroTik indicatif) ; P3 0 % par construction + QDI ; P4 exploratoire n=4 ; VoIP/JFI sortis (note).
- [ ] **Step 2: 03-partie2** — note G4-Mathis au §portes + backfill non-exclusif en annexe B (157 low dont 20 zero / 42 high bug fixé / 12 G3).
- [ ] **Step 3: 07-refs + 00-front** — +17 refs (RFC8033, BBRv2 Chor 2022, Flent 2017, WIS2.0/OMM, ARTEC, tarifs) citées en 02/03 ; résumé/abstract chiffrés (n, IC, QDI/4, débit).
- [ ] **Step 4: rebuild docx** — `node build-docx.js` (ou cible Makefile), vérifier 0 `[[ ]]` nouveau.
- [ ] **Step 5: Commit** — `git commit -m "docs(book): preuve pilote valid-only + quarantaine + cout-paliers + bornes P3P4"`.

### Task 5: Figures avec incertitude

**Files:** Modify `book/figures-lien.js` (figR2 deadline + figR3 QDI) ; Rebuild `book/figures/`.

- [ ] **Step 1:** figR2 : 3 barres BBR valid-only + IC95 (whiskers) + labels n + mention « CUBIC : quarantaine G4 » ; titre finding « CAKE tient ~98 % à 220 ms, pfifo ~60 % (P2 BBR, n=…) ».
- [ ] **Step 2:** figR3 : barres QDI + IQR + n (mêmes cellules).
- [ ] **Step 3:** Run `node figures-lien.js`, contrôle visuel SVG (pas de texte coupé).
- [ ] **Step 4: Commit** — `git commit -m "feat(figures): barres IC95+IQR + n valid-only (R2/R3)"`.

### Task 6: API additive + cache

**Files:** Modify `pkg/api/server.go` (GET /api/results, /api/quarantine) ; Test `pkg/api/stats_api_test.go` (create).

- [ ] **Step 1: failing test** — `GET /api/results` contient `iqr`, `ic95`, `n` ; `GET /api/quarantine` contient `failed_gates` ou `synthese_backfill`.
- [ ] **Step 2:** implémenter via `pkg/metrics` + cache `sync.Once`/map (précalcul au chargement, même seed) ; anciens champs intacts.
- [ ] **Step 3:** `GOPROXY=off go test ./pkg/api/ -v` PASS.
- [ ] **Step 4: Commit** — `git commit -m "feat(api): stats inferentielles + cache (additif)"`.

### Task 7: Web + TUI (parité, verdict, quarantaine, coût, VoIP/JFI)

**Files:** web `Résultats`/`Provenance` (+coût 6 paliers), TUI onglet Résultats (compact 80 cols), retirer VoIP/JFI du central.

- [ ] **Step 1:** Résultats : méd [IC95] + IQR + n ; épinglage pfifo vs cake → p + delta ; verdict auto étendu (moteur Go partagé).
- [ ] **Step 2:** Provenance : causes lisibles (failed_gates + synthèse backfill).
- [ ] **Step 3:** Coût : 6 paliers + sensibilité P2 cake|BBR ; VoIP/JFI → détail/annexe labellisée.
- [ ] **Step 4:** `GOPROXY=off go build ./...` + tests ciblés PASS (pas de `npm install`).
- [ ] **Step 5: Commit** — `git commit -m "feat(ui): parite stats + verdict + quarantaine + cout-paliers"`.

### Extension B (conditionnelle : banc Linux + créneau prod, même pipeline)

- `cgo run --profiles P2 --qdiscs pfifo_fast,fq_codel,cake --cc bbr --reps 3` (~45 min, hors prod + reprise) ; P3-1500ms 3×2 (6 events) ; `data/link_audit.csv` (fibre + Yas 4G creuse/pointe) + tableau RQ1 ; recaptures VM 1920×1080 (Web 3 vues + TUI) via `kit scan`, secrets env seuls. Rejouer Tasks 3–5 après gel (`cgo verify` vert).

## Self-Review

- Couverture spec : §3→T3–T5, §4→T6–T7, §5→T0(env)+T2+ext-B, vérif→chaque tâche. Fil recadrage→T4. P0→contrainte globale.
- Placeholders : aucun (code/textes complets, seed/valeurs explicites).
- Cohérence types : `FailedGates []string` partout ; `iqr/ic95/p/cliff` mêmes noms JS/Go/API ; seed 42 unique.
