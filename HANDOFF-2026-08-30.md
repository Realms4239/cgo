# Handoff — 30 août 2026

Ce document résume l'état de la branche `main` de C:\cgo à destination d'un agent frais.
Il remplace les artefacts précédents (superpowers/, ARG.md, certify.txt, docs/research)
qui ont été purgés du dépôt dans cette passe.

## État de la branche

- `main` ne doit jamais recevoir de commit direct (sauf `chore:` trivial).
  règle reprise de l'ancien AGENTS.md, qui a été volontairement supprimée du dépôt :
  **le processus est resté**, seul le fichier a été purgé.
- Worktree de travail : `.worktrees/<slug>` + branche `agent/<slug>`, interdiction de
  `checkout` dans le checkout principal.
- Vérification : `go vet ./...`, `go test ./pkg/... ./cmd/...` (OK au 30/08),
  frontend `bun run typecheck && bun run test` dans `web/frontend/`.
  Le Makefile historique (`make test`, `make test-real`) n'est pas disponible sous
  Windows (pas de `make` installé) : utiliser `go test ./...` et `bun run test` à la place.
- Pas de serveur en cours sur :9090 nécessitant un redémarrage.

## Ce qui a changé pendant cette passe

- **Sweep des commentaires** : ~110 commentaires Go/TS/TSX encore en anglais ont été
  traduits **vers le français** (langue du produit), via `scripts/comment-sweep.js`
  (remplacement par TSV), supprimé après usage — il n'est plus dans le dépôt.
- **Portabilité du serveur** : `cmd/cgo/main.go` — `--addr` par défaut `127.0.0.1:9090`
  (interface locale) au lieu de `:9090` (toutes interfaces). L'unit systemd
  (service install) et `kit/vm-install.sh` passent toujours `--addr 0.0.0.0:9090`
  explicitement pour l'exposition VM.
- **Purge** : AGENTS.md, .clinerules/, .superpowers/, ARG.md, certify.txt,
  docs/research/, scripts/comment-sweep.{js,tsv} supprimés (hors du dépôt).
  AGENTS.md était un artefact d'orchestration d'agent (règles de travail),
  `.superpowers/` contenait des traces de sessions AI — rien d'utile pour le produit.
- **README** : mise à jour (port local par défaut, workflow de déploiement).

## Valeurs sûres pour l'agent frais

- `git status` au 30/08/2026 : 100 fichiers modifiés (commentaires + main.go +
  README.md + suppression indices). Aucun fichier non suivi.
- `data/frozen-wave1`, `data/frozen-wave2` ne doivent jamais être modifiés.
- `docs/api.md`, `docs/deploy.md`, `docs/architecture.md` : existants, à jour.
- `pkg/doctor/doctor.go` : décrit les capacités environnantes, exécuté côté VM.
- La branche ne doit pas être poussée avant validation sur Linux (pkg/api a des
  tests d'intégration qui supposent des capacités tc/netem absentes sous Windows).
- `kit/engine.sh --action deploy` : cross-compile + pousse + installe sur la VM
  (192.168.174.128:2222, clé `kit/deploy_key`).

## État du compte-rendu final

Le dernier agent a terminé son travail (commentaires traduits, purgés, tests verts
au 30/08). Ce handoff fait office de compte-rendu : rien d'autre n'est à faire avant
de passer la main.

---

*Ce document est le seul artefact de session résiduel ; il peut être supprimé après usage.*
