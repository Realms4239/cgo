// GATE_LABELS — libellés opérateur des portes G0–G7 (SPEC §2.2).
// Source unique : partagé entre CampagneView (bandeau portes) et le tiroir
// méthode de ResultatsView — ne plus jamais de copie locale divergente.
export const GATE_LABELS = ['cible joignable', 'bulk démarré', 'sondes actives', 'latence plausible', 'débit cohérent', 'pas de doublon', 'baseline stable', 'CPU ok']
